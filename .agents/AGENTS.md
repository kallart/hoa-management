# Invoice Design Protection Rule

- **STRICT DIRECTIVE:** The file `frontend/src/pages/InvoiceDetail.tsx` (ใบแจ้งค่าส่วนกลาง) is considered **LOCKED** and its design/layout is perfectly calibrated.
- **DO NOT MODIFY** the layout, styling, PDF generation logic, or any visual components in `InvoiceDetail.tsx` without explicit, undeniable permission from the user.
- If you are asked to make changes to a global component (e.g., CSS tokens, utility classes, global layouts) that might unintentionally affect `InvoiceDetail.tsx`, you **MUST** warn the user first and ask for confirmation before proceeding.
- A backup exists at `InvoiceDetail.backup.tsx` in case of emergencies.
